package stuff.test;

import blockspace.staff.accord.nota.Nota;

import java.util.*;
import javax.sound.sampled.*;

public class Tone {

//	public static void main(String[] args) throws LineUnavailableException {
//		Tone.sound(Arrays.asList(tuneToFreq(Nota.LA), tuneToFreq(Nota.FA), tuneToFreq(Nota.RE)), 5000, 0.8);
//		Tone.sound(Arrays.asList(tuneToFreq(Nota.LA), tuneToFreq(Nota.FA + 1), tuneToFreq(Nota.RE)), 5000, 0.8);
//		Tone.sound(Arrays.asList(880, 700, 587), 2000, 0.8);
//		Tone.sound(Arrays.asList(880, 700, 555), 2000, 0.8);
//		Tone.sound(Arrays.asList(784, 700, 555), 2000, 0.8);
//		Tone.sound(Arrays.asList(784, 660, 555), 2000, 0.8);
//		Tone.sound(800,1000,0.8);
//		Tone.sound(600,1000,0.8);
//		Tone.sound(500,1000,0.8);
//		Tone.sound(400,1000,0.8);
//	}
}

